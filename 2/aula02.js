let altura = parseFloat(window.prompt("Me informe a altura do retângulo: "));
let base = parseFloat(window.prompt("Me informe a base do retâmgulo: "));

const area = base * altura;

alert("A aréa do retângulo é: "+area);